%p.text-center This is your shortened url.

.row
        .col-md-12
                %p.lead.text-center
                        %a{href: "#{@short_version}"}
                                = @short_version
